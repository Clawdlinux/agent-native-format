# Manifest Builder

Constructs ACP Execution Manifests from resolved capabilities and a tool
source. Defines the consumer-side `ToolSource` and `IDSource` interfaces so
tests can mock them with `go.uber.org/mock/gomock`.
