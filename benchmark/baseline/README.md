# MCP Baseline

The MCP baseline should use the official SDK and perform the same tasks as ACP.

No benchmark result is pitch-ready unless both paths are apples-to-apples:

- same task definition
- same LLM model
- same prompt budget
- same mock service behavior
