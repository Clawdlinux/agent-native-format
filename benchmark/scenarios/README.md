# Benchmark Scenarios

YAML task definitions for ACP vs MCP comparison.

Planned scenarios:

- `01_db_query.yaml` — S1 simple DB query
- `02_multi_tool.yaml` — S2 DB + Slack + audit
- `03_complex_dag.yaml` — S3 research/analyze/draft/review/email
- `04_scale.yaml` — S4 many registered tools, few relevant tools
- `05_auth_heavy.yaml` — S5 OAuth + API key + mTLS
