# Agent-accuracy benchmark dependencies.
#
# Installed into the repo's existing .venv:
#   python -m pip install -r benchmark/agent_accuracy/requirements.txt
#
# Pinned for reproducibility. Bump deliberately when re-running the
# benchmark; record the bump in the result's meta.json.

openai==1.55.3
anthropic>=0.40,<1.0
httpx<0.28
tiktoken==0.8.0
python-dotenv==1.0.1
PyYAML==6.0.2
