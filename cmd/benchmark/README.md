# benchmark CLI

Developer CLI for running ACP vs MCP comparisons.

Planned commands:

- `benchmark run --scenario benchmark/scenarios/01_db_query.yaml`
- `benchmark report --input results/latest.json --out results/summary.md`
