# Commands

Entrypoints for ACP services and developer tools.

- `acp-server/` — HTTP/gRPC server for `POST /v1/context` and feedback.
- `benchmark/` — CLI wrapper for benchmark scenarios and report generation.
