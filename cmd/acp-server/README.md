# acp-server

Week 1 target: boot an ACP server that can return a valid Execution Manifest
for three registered tools from an in-memory registry.

Planned endpoints:

- `POST /v1/context`
- `POST /v1/feedback`
- `GET /healthz`
