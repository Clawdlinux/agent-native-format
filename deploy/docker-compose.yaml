services:
  acp-server:
    image: ninevigil-acp/acp-server:dev
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      ACP_AUTH_TOKEN: ${ACP_AUTH_TOKEN:-dev-token}
    command: ["--addr", ":8080"]
    ports:
      - "8080:8080"
